# Decisions, source conflicts, and traceability

Status: proposed Focal decisions. The user specified Rust; RAM-primary storage with disk durability; laptop through multi-region/Meta-scale ambition; and minimal incremental complexity through six deployment stages. These requirements govern the adaptation. Nothing here asserts implementation is already complete.

The frozen [reference snapshot](reference/README.md) preserves upstream accepted wording even when conflicting or superseded. [01](01-source-audit.md) records observed Sylk behavior and static concerns. [02](02-domain-and-lifecycle.md) records domain decisions D-01 through D-08; the F-series below records system decisions.

## 1. Source precedence

1. User requirements and explicit corrections in this task.
2. The reconciled numbered Focal documents as the proposed implementation baseline.
3. Later explicit Hecate amendments over earlier conflicting Hecate passages.
4. Sylk source and tests as evidence of existing behavior and lessons, not automatic target semantics.
5. Historical bug reports as regression scenarios, not proof the current Sylk revision still has the bug.

Where Focal improves an underspecified algorithm, the improvement is labeled a design decision. A citation to Hecate does not validate pseudocode under arbitrary schedules or crashes.

## 2. System decisions

| ID | Decision | Reason and alternative considered | Contract / work |
|---|---|---|---|
| F01 | Custom RAM graph with durable segment log and immutable checkpoint/archive objects | User-confirmed storage model. Hecate's cold LSM/B-tree profiles are not the authoritative Focal state engine; an embedded database would obscure ownership and duplicate storage semantics. | [04](04-storage-and-distribution.md), P03/P04/P12 |
| F02 | One authoritative ordered log per session; range-shard its state/apply | Preserves Hecate effective-state, graph and delta order. Independent claim-log shards would change atomicity/order and require a different consistency design. | [00](00-target-architecture.md), P08–P11 |
| F03 | Durable quorum before success; one voter includes disk flush | A RAM-only replica ack cannot preserve acknowledged proof after the claimed failure set. Kafka-like durability does not imply Kafka implementation or ack policy. | [04](04-storage-and-distribution.md), P04/P08/P13 |
| F04 | Normal mutation success waits published visibility; read tokens expose sequence | Distinguishes durable from readable state and closes apply-lag surprises. Explicit async receipts can be added later with a different named contract. | [03](03-rust-workspace-and-interfaces.md), P04/P11 |
| F05 | Safe serial reducer is permanent oracle; tracked deterministic parallel apply | Hecate's atomic-max/safety-net sketch does not by itself establish serial equivalence. Full conflict audit and recomputation preserve one meaning. | [04](04-storage-and-distribution.md), P02/P10 |
| F06 | Maintained Rust runtime, Raft and QUIC adapters initially | User requires custom storage, not an entirely custom runtime/consensus/cryptographic transport. Hecate runtime/transport are docs, not reusable binaries. | [03](03-rust-workspace-and-interfaces.md), P00/P07/P08 |
| F07 | Owned mutable core; safe arenas first; library/internal immutable sharing permitted | Preserves writer ownership without an unenforceable blanket dependency ban on reference counting. Unsafe optimizations require measured need and proof. | [03](03-rust-workspace-and-interfaces.md), P03/P10 |
| F08 | Log-derived immutable deltas; no separate outbox | Removes Sylk cross-journal delivery gaps and current-state historical rehydration. Consumers own durable cursors. | [01](01-source-audit.md), P07 |
| F09 | At-least-once recovery delivery plus durable idempotent logical outcomes | Cursor resume and dedup do not make arbitrary external effects exactly once. Explicit effect reconciliation closes the crash gap. | [00](00-target-architecture.md), P06/P07 |
| F10 | Region topology compiles explicit failure/residency intent | Region names and replica count alone cannot promise survivability. Placement must cover voter quorum and evidence/checkpoint bytes. | [04](04-storage-and-distribution.md), P13 |
| F11 | One engine and additive deployment concepts | “Stepped complexity” is an acceptance requirement. No laptop/cluster/regional semantic modes or mandatory expert storage knobs. | [08](08-stepped-complexity-and-deployment.md), P00/P15/P16 |
| F12 | Indefinite immutable proof custody initially; bounded hot/log/cursor windows | Complete proof survives retirement. A future destructive proof-deletion policy must be explicit and cannot arise from memory pressure or WAL compaction. | [04](04-storage-and-distribution.md), P12 |
| F13 | Stable opaque object IDs plus separate versioned canonical content hashes | Keeps stable references and cyclic graph construction tractable while preserving content-identical no-op behavior. Content hash and physical/allocated identity are separate. | [02](02-domain-and-lifecycle.md), P01/P02 |
| F14 | Archive-backed identity plus request-generation fences | Bounded hot dedup cannot guarantee lifetime identity or reject arbitrarily old random request IDs without durable information. Generation floors and verified archive lookup define safe expiration. | [03](03-rust-workspace-and-interfaces.md), P02/P12 |
| F15 | Root/regional control metadata outside steady-state claim commits | Fleet scale cannot depend on one global session directory write or membership broadcast per claim. Root stores delegation; regional/session groups own local work. | [00](00-target-architecture.md), P09/P13 |
| F16 | Native operations ride a negotiated wire profile carrying `FCNINPUT` frames | CLI, JSON/YAML, MCP and embedded clients submit byte-identical frames and the owner decodes exactly what was journaled, so exact retry and the recorded intent share one identity. Re-encoding typed documents on the server would create a second admission path and a second identity. | [19](19-cli-mcp-implementation.md), [21](21-native-input-format.md), P17 |
| F17 | Native frames are compiled beside the hosts, in a crate that depends on `focal-core`; `focal-client` stays core-free | The planned extraction of the input codec into its own crate was surveyed and deferred: the codec is coupled to admission helpers (artifact views, response budgets, profile checks, source plans), so moving it would move admission machinery rather than a leaf. In production neither `focal-client` nor `focal-mcp` depends on `focal-core` (test-only), so native documents stay in `focal-client` and the document-to-frame compiler, frame encoder and native operation journal live in `focal-native-client`, used by the node's CLI and MCP hosts. The codec extraction remains open and is recorded, not hidden. | [03](03-rust-workspace-and-interfaces.md), P17 |
| F18 | The native session checkpoint retains the activation record's Raft index; genesis activation defaults to the authored profile | A restored replica must report the exact activation position rather than a bound derived from the sealed legacy prefix. Fresh ledgers author their claims natively, so genesis activation (fleet admin or offline laptop) uses `AuthoredV1`; a populated prefix is imported projection-only because legacy claims carry no authored native content. | [22](22-native-record-format.md), [23](23-native-activation-and-import.md), P17 |
| F19 | Native mutations do not ride managed request streams | The managed receipt vocabulary (`crates/focal-model/src/managed.rs`) is an imported frozen source whose persisted receipts cannot gain a native outcome, and the native owner already resolves exact retry from the frame's request identity. Native operations are journaled by the client as the exact frame, retried verbatim, and reconciled through the committed outcome read; managed streams keep serving legacy commands. | [19](19-cli-mcp-implementation.md), [21](21-native-input-format.md), P17 |
| F20 | A native operation's durable identity is its request identity (`n1:` reference), journaled with the exact frame and the owner-side intent fingerprint | The native owner resolves exact retry from the frame's request key before admission, so no epoch registration precedes a native mutation: the request epoch is fixed at one and the operation reference is the request ID. The journal (`crates/focal-client/src/native_store.rs`) claims the identity under the canonical document before any object identity is minted, persists the compiled frame and its fingerprint, resends the identical bytes on retry, and records only a receipt whose invocation and intent equal the journaled frame. Replies that are pending or refused never advance the journal. | [19](19-cli-mcp-implementation.md), [21](21-native-input-format.md), P17 |
| F21 | Native descriptors share verb names with the V1 catalog at version 2; a host exposes one catalog per ledger, chosen by the active engine, and the coverage table names every unexposed operation | Humans and agents keep the verbs they know (`claim.submit`, `testament.submit`, `validation.begin`, …) while the documents, wire profile and retry identity differ; mixing both catalogs in one listing would make discovery ambiguous. The R4.0 coverage table (`crates/focal-client/src/operations/native_inventory.rs`) is an exhaustive match over the owner's operations, so an operation without an authored surface is a declared `WireOnly` row a test pins, never an accidental omission. | [19](19-cli-mcp-implementation.md), P17 |
| F22 | The standard native session limits are proportionate to the record-buffer envelope, and every host proves it | Completion-class admissions fund their future record buffer from the owner's shape limits against the encoding envelope; the default owner limits (batch 4,096, 4,096 evaluations per claim, 4 MiB preparation) exceed the 4 MiB envelope, so every receipt, work and testament admission on a node was refused as capacity while the qualified suites used bounded limits. `NativeSessionLimits::standard` now carries bounded shapes (batch 256, 64 evaluations per claim, 1 MiB preparation, 64 plan nodes) and a ledger test runs the complete cycle under exactly those limits. | [18](18-lifecycle-storage-upgrade.md), [22](22-native-record-format.md), P17 |
| F25 | Trusted timers are scheduled by a due-timer index row whose existence every reader can derive | A node must find the deadlines due at its clock without scanning claims, evaluations or monitors, must never redeliver a consumed timer, and must not need any memory across a restart. The `DueTimer` family ([22 §7](22-native-record-format.md)) keeps one unit row per undelivered claim, monitor and evaluation deadline, ordered by logical time. A row exists exactly while the timer has not been delivered and its target is still live: the retained outcome row of the timer's invocation is the consumption fact every reader (leader, replay, checkpoint) can see, so a delivered timer whose target row did not change still carries the deletion of its row, and the node's sweep rescans the index each tick and after every restart. A claim's terminal transition leaves its timer row so ordinary transitions never pay for claim timers; the timer fires once on the terminal claim, records its outcome and retires the row. Timer rows are funded per operation (creation, registration, the report's own evaluation, cohort seals, graph consequences, the timer's own consumption) and admitted by write envelopes as deletions. The sweep delivers at most sixty-four timers per tick through the three trusted owner entrypoints, only on the leader, and reports refusals rather than looping. | [22](22-native-record-format.md), [17](17-lifecycle-state-and-authority.md), P06 |
| F26 | One continuous stream sequence line per ledger; native deltas are derived from committed events under delta schema 2 | Cursor positions, delta identities and retention floors are `SessionSeq` values, and a watch registered before activation must continue without a resync. The line is the sealed legacy prefix `0..=N` followed by the native records: a genesis ledger's native record `s` is stream sequence `s`; an imported ledger's native sequence one is the image of the legacy prefix and record `s > 1` is `N + s - 1` ([23 §6](23-native-activation-and-import.md)). The legacy tail keeps its retained schema-1 deltas; every later delta is built on demand from the committed native events (`Key::Event` rows, retained with the prefix) as `DeltaFact::Native` carrying the exact event record, the nearest legacy lifecycle action for coarse consumers, the request principal (zero for timers and the import) and the claim the fact concerns. The frozen V1 durable codec refuses the native variant, so no legacy tail ever holds one. A native ledger streams only under the native wire profile, which proves the consumer decodes schema 2; older profiles are refused at the door. The native engine keeps no historical read snapshot, so a seeded watch reads its own seed through linearizable native reads after the snapshot is pinned (at-least-once between snapshot and seed) and the tail from the snapshot; cursor receipts name the published end of the line at apply, identically on every replica. | [22](22-native-record-format.md), [23](23-native-activation-and-import.md), [19](19-cli-mcp-implementation.md), P17 |
| F34 | Node liveness is a SWIM failure detector with the Lifeguard enhancements, under-load deadline extensions and Vivaldi coordinates, whose outcomes the partition owner commits | A committed placement guarantee is only as good as the fleet's knowledge of which hosts answer; a private opinion of one node would let two controllers disagree, and static timeouts would either accuse distant or loaded hosts falsely or wait seconds on local ones. The detector runs on every node, suspects only confirmed peers, scales every timeout by its own local health and by the estimated round trip from network coordinates, lets a loaded host ask for bounded, decaying extensions witnessed by its own progress, and commits the outcome as a directory fact the planner, the healer and the guarantee report consume ([24](24-placement-execution-and-fleet-control.md) §12) | [24](24-placement-execution-and-fleet-control.md) §12, P08, P09, P13 |
| F33 | A node's directory identity is its enrolled key, and a credential is renewed under that key with the replaced certificate retired after a bounded grace | Binding proofs and grants to a certificate fingerprint would make every renewal a re-enrollment with a new generation, invalidating group grants and placement progress; binding them to the key the authorized certificate carries lets a renewal change nothing in the directory. The registry keeps the replaced certificate authorizing for the sponsor's grace so work in flight completes and a holder that crashed before installing the renewal can still reconnect and converge on the committed one ([24](24-placement-execution-and-fleet-control.md) §11); the transport keeps authenticating by certificate fingerprint and learns the new one when the node presents it | [24](24-placement-execution-and-fleet-control.md) §11, P07, P09 |
| F32 | Disk is admitted through one shared envelope per volume that lives in `focal-memory`, and a tenant is admitted to a node when a committed placement first assigns it a session | Every durable owner (log writer, content store, checkpoint path) must refuse before it acknowledges, and they sit below any archive crate in the dependency graph, so the envelope lives beside the memory budget in `focal-memory` rather than in the planned archive crate; its counters are shared by owners on different threads, the one boundary [10](10-ownership-and-failure-policy.md) already justifies for the memory budget. Tenants are not configured per node: the placement that assigns a tenant's session is the admission request, answered under `node.max_tenants` and the node budget, and a node that cannot fund the tenant records a capacity refusal against its own assignment instead of hosting it unfunded ([24](24-placement-execution-and-fleet-control.md) §10) | [24](24-placement-execution-and-fleet-control.md) §10, [04](04-storage-and-distribution.md) §5, P09 |
| F31 | Placement progress is committed directory state derived from signed readiness, and the pre-release control-plane formats advance rather than fork | A controller that kept assignment progress in memory would lose it at every leader change and could not be inspected; the partition now records one monotone progress row per copy, bounded refusals, the copies an activation leaves behind and a measured guarantee report ([24](24-placement-execution-and-fleet-control.md)). Custody and promotion are never asserted by the controller: readiness signed by the copy itself raises a row to `CustodyVerified`, and the cutover fence is recorded only once every voter is promoted. The partition checkpoint becomes schema 2 (schema 1 restores through a typed conversion that invents no custody), the control checkpoint schema 4 and the command envelope schema 2; none of these formats shipped, and a partition group bootstrapped at schema 1 is not the same group at schema 2 | [24](24-placement-execution-and-fleet-control.md), [04](04-storage-and-distribution.md) §13, P10 |
| F30 | The peer verbs are authored shapes of `claim.submit`, and corrections and follow-ups derive every identity from the facts they rest on | A second workflow engine for challenges would duplicate admission, journaling and recovery; instead `claim.challenge`, `claim.consult`, `claim.correct` and `claim.follow_up` are typed documents lowered to one claim document before the shared compiler runs, so they share the frame, the `n1:` identity, the receipt and the coverage table row of `claim.submit`. A correction's and a follow-up's occurrence, claim, validation and timer identities are keyed BLAKE3 digests of the ledger, the author and the followed claim, verdict and text, so identical facts yield byte-identical descriptors and the owner's content identity resolves a repeated delivery to one committed claim without a reservation step. `claim.lineage` and the native `claim.wait` are client-composed bounded reads over the existing wire, not new owner reads. | [19](19-cli-mcp-implementation.md), [21](21-native-input-format.md), P17, P19, P20 |
| F29 | Peer follow-ups relate to the claim they follow; they never reopen it | A challenge whose verdict failed and a consultation that was answered are terminal, and a terminal or released claim cannot own a child (P17.12), so a correction and a follow-up consultation are ordinary authored claims linked by `invalidates` and `refines` to the claim they follow, under whatever enclosing cause their author holds. The owner gates them by that claim's authored policy (`corrective_allowed`, `escalation`, `single_issuer`, `max_follow_ups`) and, for a correction, by the exact report of the terminal negative verdict at the challenge's current registration generation, cited as `reviews` evidence; the relation index makes both listable and bounds the single-issuer and follow-up counts. `caused_by` keeps its live-parent ownership meaning, with the same escalation vocabulary deciding who besides the issuer may cite a parent. | [21](21-native-input-format.md), [17](17-lifecycle-state-and-authority.md), P16, P17 |
| F28 | Peer follow-up policy and exact evidence are authored claim content, not a separate policy artifact | A challenge's or consultation's follow-up rules must be immutable, hashed with the claim and readable by every owner without a second lookup; the plan's `peer-policy` artifact would need standalone artifact registration the native engine does not offer and a second admission read. Descriptor schema 2 therefore carries an optional `PeerPolicy` (`corrective_allowed`, `max_follow_ups`, `single_issuer`, `escalation` none/holder/evaluator) after the deadline, and `reviews`/`derived_from` relations may target exact evidence (`RelationTarget::Evidence(ArtifactRef)`, input tag 4, canonical tag 5), admitted only when that artifact is committed at that descriptor hash. Schema-1 identities, bytes and hashes are untouched; the V1 codec refuses the new target; `ByRelation` indexes evidence targets by artifact identity so corrections and reviews of one artifact are listable. | [21](21-native-input-format.md), [22](22-native-record-format.md), P16, P20 |
| F27 | Managed generations are bounded by ordinal count and recycled under one slot-generation watermark | A principal's request stream must stay finite on both sides without time-based deletion: a generation issues at most its rotation bound of ordinals, and once every ordinal through the bound is retired the client stops issuance durably, closes exactly, records the retired fence, removes the old store and registers again above the generation the slot presents. The registry evicts only vacant pairs, the longest-closed first, and presents `max(last generation, watermark)` on every vacant pair so a registration is assigned exactly one above what it cited (the frozen client rule) while still lying above every generation the registry ever issued; the watermark is persisted in `FOCALSS7` and derived from the retained pairs of an SS6 checkpoint. Occupancy reads answer only for the authenticated principal's own pairs. The rotation bound is part of the coordinator record; campaigns lower it with `FOCAL_MANAGED_ROTATION`. | [15](15-managed-request-streams.md), [13](13-cli-and-agent-implementation-plan.md), P17 |
| F24 | Native index families are ordinary recorded rows, funded per operation and rederived by every reader | Bounded lists need ordered key ranges per predicate. The thirteen families ([22 §7](22-native-record-format.md)) are unit rows of the same range: derived from the exact primary rows the leader writes, required exactly by replay validation, rederived by checkpoint validation and written by the import image, so no reader trusts an index it did not check. Index rows count toward the batch and toward every completion and respondent promise; a promised artifact's inputs are bounded by `NativeLimits::artifact_inputs` and narrowed by a small batch instead of refusing every report; the range envelope prices deletions by declared heap (`deleted_heap`) so an index deletion is not priced as the largest entry; the standard record envelope grows to 6 MiB to hold a preparation-sized body plus every fixed row. Only `ByStatus` changes after creation; artifacts, definitions and testaments carry no creation row. Lists are stateless with node-authenticated continuations that name the last visited row. | [22](22-native-record-format.md), [19](19-cli-mcp-implementation.md), P03, P07, P18 |
| F23 | One native driver for every host, one journal per adapter, and explicit consumption of MCP results | The CLI and the MCP adapter share `focal-native-client`'s driver (resolve, compile, encode, fingerprint, journal, drive) with only the blocking read and cancellation supplied by the host, so identical documents yield byte-identical frames and one fix serves both. Each adapter owns its `n1:` journal (`client/native`, `client/mcp-native`) as the V1 request stores do, so neither can consume the other's identities; cross-adapter observation is the owner's outcome read by request key, not a shared file. MCP results stay listed until acknowledged because a tool reply can be lost between the adapter and the agent; the CLI marks delivery on a successful print because its reader is the process that printed. The engine probe runs on the worker runtime before the catalogue is built. | [19](19-cli-mcp-implementation.md), P17 |

## 3. Reconciled Hecate gaps and contradictions

| Source issue | Why it cannot be copied literally | Focal resolution |
|---|---|---|
| LEDGER §3 happy-path status list omits failures required by LEDGER_CORE §4 | No exhaustive reducer or wire enum can be built from it | [02](02-domain-and-lifecycle.md) defines complete active/terminal vocabulary, transition table and refusal cases |
| LEDGER §4 describes incremental SCC machinery; LEDGER_CORE §3 explicitly rejects a global SCC monitor | Conflicting monitor ownership/algorithm | Per-parked-scope closures with SCC condensation and independent brute-force oracle |
| Rank refers to `invalidates` while LEDGER relation enumeration omits it | Security classification cannot match an unregistered string | Closed `Invalidates` variant plus exhaustive override classification |
| Earlier protocol ADR text refers to TCP; later PROTOCOL mandates QUIC | Different wire assumptions | Focal authenticated framed QUIC; no claim of Hecate byte compatibility |
| STORE generic data shard has its own log; claims-specific clauses retain one session log | Applying generic sharding to claims destroys the chosen session order | Claims partitions consume one session log; directory/metadata groups have separate authority |
| MATERIALIZER §5 mentions HRW while later STORE requires one range directory | Two independent splitters create mismatched ownership | One range map for authoritative state/apply; cache placement is separate and nonauthoritative |
| MATERIALIZER conflict detection depends on overlay observations while workers race | Smaller-index writers may not have executed yet; stale reads can evade a local check | Complete tracked access audit at deterministic barrier; recompute speculative suffix/epoch |
| MATERIALIZER read+write DAG example can add self-edges | Read-modify-write can deadlock its own node | Exclude self-edges, deduplicate predecessors, test RMW explicitly |
| Max-writer-index overlay drops earlier versions | Earlier log entries may need earlier values; write-write resolution alone does not fix read dependencies | Per-entry writes/multiversion overlay plus serial-order validation |
| Deferred entry with already computed successors | Successors may contain stale dependent computations | Discard and recompute all affected results, including indirect dependencies |
| Epoch size derived from local timing but claimed replay deterministic | Replica measurements can disagree | Version sizing inputs or prove epoch selection cannot affect state/delta outcome |
| Cross-node apply called “no 2PC” without visibility protocol | Common order does not prevent torn reads or partial apply after crash | Deterministic participant batches, recovery replay, fixed-prefix publication barrier |
| Split described as seed/tail/directory CAS | Source may keep accepting work after destination cutoff | Logged stop/activation barrier and epoch fencing before directory publication |
| Watch no-gap/no-duplicate language conflates transport and effects | Lost acknowledgements cause replay; unrelated sequence clocks are not comparable | Session sequence + ordinal cursors, explicit redelivery/Resync, idempotent consumer effects |
| “Read-your-watch” assumes current read barrier is at least received delta | Client may route to a lagged/moved reader | Carry minimum sequence across retries and wait for the requested publication prefix |
| “Small” sequencer lifecycle projection | Its size can grow with a large live session | Measure and budget projection; asynchronous versioned proofs for nonlocal admission; declare ceiling |
| Monitor closure budget “alarms” after breach | Alarm alone does not bound allocation | Reserve before registration; reject/pressure before unsafe growth |
| Idle-time or unspecified retirement cadence | Saturated systems might never reclaim | Paced debt-based custody/retirement with reserved control capacity |
| Durable log but artifact replicas not in failure contract | Committed proof could reference unavailable/lost evidence after region loss | Verify evidence custody under the same promised failure set before close |
| Minimum/maximum watermarks across different clocks | Per-shard indexes and physical offsets cannot be compared directly | Typed clock domains and explicit mapping; common session prefix for graph visibility |

The amended algorithm is motivated by the references, not a claim to have implemented Calvin or reproduced a paper's throughput. Deterministic sequencing and declared-access execution have precedent in [Calvin](https://www.cs.yale.edu/homes/thomson/publications/calvin-sigmod12.pdf); Focal's precise retry, memory and publication rules remain obligations of its own implementation.

## 4. Requirement-to-work-to-test mapping

| Requirement | Architecture | Work | Verification |
|---|---|---|---|
| Rust project | 03 | P00–P01 | Build/lint/visibility and canonical fixtures |
| Claims/testaments/artifacts/validators | 02 | P01/P02/P05 | T01–T06, T12–T14 |
| Generated work not yet actionable | 02 | P02/P06 | T05 |
| Immutable content, correction lineage | 02/03 | P01/P02 | T02/T03/T14 |
| Writer and execution fencing | 02/04 | P02/P06/P08 | T04/T14/T20 |
| RAM-primary custom storage | 03/04 | P03/P10/P11 | T07/T08/T23/T30 |
| Disk-backed durability | 04 | P04/P08/P12 | T09–T11/T26 |
| Graph cycles and no lost wakes | 02/04 | P06/P11 | T15–T17/T25 |
| One session order with sharded apply | 00/04 | P08/P10/P11 | T20/T23–T25 |
| Bounded long-running memory | 04/06 | P03/P09/P12/P14 | T19/T22/T26/T30 |
| Laptop to multi-node | 00/08 | P04/P07/P08 | T09/T20/T21/T32 |
| Kubernetes without new ledger semantics | 08 | P15/P16 | DC07/DC08 and T32 |
| Multi-AZ and region survival | 00/04/08 | P13/P14 | T12/T27 |
| Global control scale | 00/04/08 | P09/P13/P14 | T22/T27/T30 |
| Minimal intuitive deployment increments | 08 | P00/P15/P16 | DC01–DC20, T32 |
| Operational recovery and upgrade | 06/08 | P15 | T10/T11/T26/T29 |
| Ergonomic manual CLI with equivalent flags/JSON/YAML | 11/13 | P17/P18 | Golden typed-input equivalence; real binary/client tests; malformed-input bounds |
| Optional filters on every object list | 11/13 | P17/P18 | Unfiltered and filtered fixed-prefix pages, zero-match advancement, tenant isolation |
| Artifact bytes and complete validation results | 11/13 | P17/P18 | Immutable testament manifest lookup, verified/resumable transfer, historical run/attempt/verdict reads |
| Complete cluster CLI with stepped complexity | 08/11/13 | P15/P16/P18 | Six actual operator journeys; plan/apply/resume; no enrollment-as-placement shortcut |
| Skills and MCP over one operation contract | 12/13 | P17/P19 | Schema/capability conformance, adapter parity, exact retry after restart |
| Proof-bearing challenges and corrective work | 02/12/13 | P05/P06/P17/P20 | Missing/invalid proof, authorized corrective lineage, duplicate outcome and restart |
| Satisfactory consults and linked follow-ups | 02/12/13 | P06/P17/P20 | Nested consults, partial results, terminal races, no automatic timeout correction |

T-identifiers are defined in [06](06-verification-and-operations.md); DC-identifiers in [08](08-stepped-complexity-and-deployment.md). Package acceptance adds concrete cases beyond this index.

## 5. Inputs resolved now and measured later

| Topic | Resolution / implementation default | Remaining task |
|---|---|---|
| In-memory versus volatile | Resolved by user: RAM primary, disk durability | Implement P04/P08 guarantees |
| Deployment ambition | Resolved by user: laptop to global/Meta-scale goal | Qualify actual supported envelopes at P14 |
| Complexity progression | Resolved by user: minimal next-step concepts/config | Measure six journeys at P16 |
| Ordering model | Preserve Hecate session-wide order | Revisit only if measured workload needs a new semantic partition model |
| Runtime and transport | Proposed maintained Rust adapters | Pin/qualify concrete versions at P00/P07 |
| Hash and IDs | Stable opaque IDs; versioned BLAKE3 content identity consistent with Hecate's hash choice | Commit exact canonical byte fixtures at P01 |
| Replica geography | Per-deployment declared failures and residency | Compile/validate placement; no universal region-count default |
| SLOs and target hardware | Workload/deployment inputs, not blockers to code | Record measured local baseline then named regional/global targets |
| Destructive proof retention | Not enabled in initial design | Separate explicit product/authority decision if requested later |
| External effect exactly once | Unsupported without idempotent/reconcilable effect owner | Expose honest adapter contract and indeterminate result at P06 |
| Byte compatibility with Hecate/Sylk | Not promised | Optional import/interoperability adapters require their own tested schema mapping |

No missing deployment size justifies stopping the implementation plan. Defaults cover local use; users provide additional intent only when the next use case requires it. Scale and safety claims must follow measurements and active placement state.

The [2026-09-05 CLI research](11-cli-spec-research.md) and [agent workflow audit](12-agent-tools-and-workflows.md) add explicit Focal choices. `--target` means subject and `--source` means issuer, with singular filtered reads rejecting ambiguity. They are not endpoint selectors. The user's challenge proof and consult work-quality requirements supersede weaker receipt-only source defaults. Corrective issuance remains an authorized, durable workflow with evidence; consultation normally produces targeted follow-up work. Hecate's later accepted `SKILLS_API` amendment treats optional TS/Python bindings as authoring-time document generators, so a Rust CLI/MCP/skill interface does not require an embedded script runtime. P17–P20 implement these choices without changing earlier persisted command ordinals or admitting caller-supplied lifecycle authority.

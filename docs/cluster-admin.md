# Cluster administration

Cluster commands use the selected physical node's private local admin socket. The operating-system owner authenticates this connection. A named Unix client context selects that context's node; an enrolled or remote QUIC client context cannot acquire local administration. MCP advertises the `cluster.*` tools only when this local backend is installed. Ordinary participant credentials retain their existing authority.

The root metadata group and each installed application replica have separate configurations. Root membership changes do not assign application placement. Application membership changes do not certify retained artifacts, completed custody transfer, or achieved durability policy.

## Root metadata and enrollment

| CLI | MCP tool | Result |
| --- | --- | --- |
| `cluster status` | `cluster.status` | Quorum-read root configuration and leader information |
| `cluster placement` | `cluster.placement` | Every directory partition this node acts on as its placement agent last observed it: nodes with enrollment, liveness and load; each session's placement, epochs, desired and achieved guarantee, what blocks it, pending plan progress and retiring copies. A read of committed facts, bounded per partition; every node's announced `region`/`zone` labels and every session's `residency` and `home_regions` as labels ([24 §22](archictecutre/24-placement-execution-and-fleet-control.md)); every node's committed contact as `advertise` (the address) and `endpoint` (the name its operator gave, if any; [24 §24](archictecutre/24-placement-execution-and-fleet-control.md)); every node's `credential` (`active` while the registry authorizes it, `retired` once revoked or expired, `unknown` when the registry is unreadable; [runbooks/expired-credentials](runbooks/expired-credentials.md)) |
| `cluster plan` | `cluster.plan` | The bounded next actions the placement controller would take from the committed directory (begin, install, add or promote a learner, cut over, activate, drain, retire, replan, split, merge). An explanation, never a change |
| `cluster tenants admit --tenant ID` | `cluster.tenants.admit` | Founder only: admit a tenant the cluster serves as a committed enrollment fact; every node's grants and local socket name it without a restart, and an admitted tenant reads as done |
| `cluster tenants list` | `cluster.tenants.list` | The founder's tenant and every admitted one at the committed enrollment revision |
| `cluster sessions create --tenant ID --name NAME` | `cluster.sessions.create` | Create an application session on this node for the founder's tenant or an admitted one; the same name is the same session (`existing` on retry). The reply names the ledger, its log group and the node; the agent registers and places it like the founder's |
| `cluster sessions plan --tenant ID --session ID [--survive node\|zone\|region] --max-failures N [--dry-run]` | `cluster.sessions.plan` (v2) | Plan the session's placement under the requested durability from the committed directory; the controller executes it unattended. The reply names the plan (`planned`), the plan already under way (`pending`) or that the active placement already provides it (`satisfied`); exact on retry. With `--dry-run` (`dry_run`) the same proposal is reported and nothing is journaled; `deployment plan` composes one per session |
| `cluster node metrics` | `cluster.node.metrics` | The node's metrics as Prometheus text exposition ([24 §23](archictecutre/24-placement-execution-and-fleet-control.md)), sampled by the service every five seconds: memory and volume envelopes, WAL counters, every hosted replica's leader, term, indices, apply lag, sequence, pending proposals, log kept beyond the checkpoint, retention floor and cursor lag, pending seeds and objects, the directory's route and placement epochs and achieved durability, peer delivery counters, the failure detector's members and counters, credential expiry, the placement agent's intents and admission, and the upgrade fence, every series under fixed `node`, `cluster`, `region` and `zone` labels. The CLI prints the text as it is; MCP returns it in `text`. The same text answers `GET /metrics` over HTTP/1.0 on `node.metrics_listen` when configured (loopback only, read-only, unauthenticated) |
| `cluster node readiness` | `cluster.node.readiness` | The four readiness probes with their facts ([08 §9](archictecutre/08-stepped-complexity-and-deployment.md)): `alive`, `catching_up`, `authoritative`, `policy_satisfied`, the root replica's progress and every hosted replica's leader, prefix, pending seed or import, and the directory's desired and achieved durability for it. A local read, not a quorum |
| `cluster node probe --check alive\|catching-up\|authoritative\|policy` | CLI only (a supervisor's probe) | Exit 0 with the readiness report when the check holds, exit 1 (`probe_failed`) otherwise |
| `cluster nodes list` | `cluster.nodes.list` | Committed node contact announcements |
| `cluster nodes drain --node N` | `cluster.nodes.drain` | Re-issue the node's grant ineligible at its next generation ([24 §19](archictecutre/24-placement-execution-and-fleet-control.md)); the controller heals every placement that named it and retires its copies. The founder is never drained; a node already drained commits nothing (`changed: false`) |
| `cluster nodes undrain --node N` | `cluster.nodes.undrain` | Re-issue the node's grant eligible again |
| `cluster nodes remove --node N` | `cluster.nodes.remove` | Remove a drained node no session names any more: its root-group membership when it has one, then the credential its invitation issued. Refused while eligible (`not_drained`) or holding copies (`node_holding`); a repeat resumes |
| `cluster nodes replace --node N --with M` | `cluster.nodes.replace` | Drain `N` once `M` is enrolled, alive, eligible and reporting (`node_not_ready` otherwise); the planner chooses among every eligible node |
| `cluster membership show` | `cluster.membership.show` | Root configuration and applied prefix |
| `cluster membership add-learner --node N` | `cluster.membership.add_learner` | Durably applied configuration receipt |
| `cluster membership promote --node N` | `cluster.membership.promote` | Durably applied voter promotion after actual catch-up |
| `cluster membership remove --node N` | `cluster.membership.remove` | Durably applied removal |
| `cluster membership leave-joint` | `cluster.membership.leave_joint` | Durably applied joint-configuration exit |
| `cluster leader transfer --node N` | `cluster.leader.transfer` | Transfer initiation; inspect the leader afterward |
| `cluster request inspect` | `cluster.request.inspect` | Latest local root admin intent or saved receipt |
| `cluster request retry A1` | `cluster.request.retry` | Retry the identical retained intent |
| `cluster request reconcile A1` | `cluster.request.reconcile` | Recover its receipt or evaluate its committed precondition fence |
| `cluster invite --node NAME --output FILE` | `cluster.invite` | Private invitation for one physical node; `--output -` writes it to standard output for a pipe (a packaged founder, [24 §24](archictecutre/24-placement-execution-and-fleet-control.md)). The invitation names the founder as it advertises itself — a name when the founder was started with one — so it outlives the founder's address |
| `cluster client invite --name NAME --output FILE` | `cluster.client.invite` | Private invitation for an Actor client |
| `cluster invitations list` | `cluster.invitations.list` | Redacted committed invitation page |
| `cluster invitations get ID` | `cluster.invitations.get` | Redacted invitation status |
| `cluster invitations revoke ID` | `cluster.invitations.revoke` | Durable revocation of the invitation and its issued credential |
| `cluster credentials get --invitation ID` | `cluster.credentials.get` | Issued credential metadata, without private keys |
| `cluster credentials revoke --invitation ID` | `cluster.credentials.revoke` | Durable revocation through its issuing invitation |
| `cluster credentials renew` | `cluster.credentials.renew` | Renew this node's own credential now: the same key under a fresh certificate and lifetime, installed and presented on the listener, the peer pool and the placement agent before the reply; a node also renews itself ten days ahead of expiry, and a retry after an interruption converges on the committed renewal. The founder's identity is its bootstrap authority's certificate and is not renewed. The reply names `key_identity` and `rotations` |
| `cluster credentials rotate` | `cluster.credentials.rotate` | Rotate this node's own credential to a fresh key under the same identity ([24 §11](archictecutre/24-placement-execution-and-fleet-control.md)): the node stages a key, the sponsor issues for it on the current credential's proof, the node adopts the receipt over the key it held and presents it everywhere before the reply; the previous certificate authorizes through the grace; the root re-grants the node under its new key at the next generation. A retry after an interruption adopts the committed rotation from the staged key. The founder's identity is not rotated |

Membership mutations and transfers accept `--expected-configuration-index`. Revocations accept `--expected-revision`. If omitted, the adapter obtains a current quorum view and saves or submits the resulting exact precondition. A concurrent change can reject that precondition; the adapter never silently changes a saved mutation. Invitation pages accept `--after`, `--limit` (1–64, default 32), and `--expected-revision` for continuation consistency.

Issuing invitations requires the founder's signing backend. A node invitation enrolls a physical Node; a client invitation enrolls an Actor and creates no physical node identity. Use `context enroll NAME --invite-file FILE` to retain the client's private enrollment state, then select it with `--client-context NAME` or `context use NAME`. The saved client key remains private. Issuing or revoking a credential does not change consensus membership.

## Local operator inspection

| CLI | MCP tool | Observed value |
| --- | --- | --- |
| `cluster node identity` | `cluster.node.identity` | The serving physical owner's immutable identifiers |
| `cluster node health` | `cluster.node.health` | Root-owner progress and installed/running fleet counts |
| `cluster node config` | `cluster.node.config` | Validated saved listen/advertise addresses and root namespace |
| `cluster replicas diagnostics [--session ID]` | `cluster.replicas.diagnostics` | Actual application commit/applied prefix, pending work/checkpoint, compiled managed decoder, durable required decoder, managed and native activation, a retained delivery (`delivery_retained`) and the seed chunks a seeded checkpoint still lacks (`log_entries_since_checkpoint` (the log kept beyond the last snapshot, checkpointed by cadence), `retention` (a native session's floor: `published`, `cursors`, `archived`, `floor`, `blocker`, and the archive's counts `retired`, `retiring`), `seed_chunks_missing`, [25 §5](archictecutre/25-parallel-materialization-and-ranges.md)) |
| `cluster retention show [--session ID]` | `cluster.retention.show` | A native session's retention floor and archive counts ([26 §3–4](archictecutre/26-custody-archive-retention-and-restore.md)): `published`, `cursors` (the complete sequences every registered consumer allows to retire), `archived` (the prefix the archive reports holding), `floor`, `blocker` (`cursors` or `archive`), `retired` (families retired through the applied prefix), `retiring` (a retirement record in flight). No reclamation is initiated; `null` for a session that is not native |
| `cluster archive show --claim ID [--session ID]` | `cluster.archive.show` | A retired claim's continuation and its bundle as this node holds it ([26 §4](archictecutre/26-custody-archive-retention-and-restore.md)): the bundle's content root and length, the prefix it claims, the final status and the retirement's sequence, `verified` once the bundle read from this node's content store passed its structural check (digest, header, every row in order), its `root` and `members`, `rows` by `families`, and `receipts` (the placement's copies holding a receipt for the bundle). `verified: false` when this node does not hold the bundle or it fails its check; `archive: null` when the claim has no continuation on this replica. Retirement is driven by every node's archive agent (`FOCAL_RETIRE_INTERVAL_MS`, 5,000 by default; `FOCAL_RETIRE_AFTER_MS`, the grace a finished claim stays readable in the core, 86,400,000 by default) |
| `cluster gc show` | `cluster.gc.show` | The collector on this node ([26 §5](archictecutre/26-custody-archive-retention-and-restore.md)): its `config` (`interval_ms`, `grace_ms`, `quarantine_ms`, `terminal_grace_ms`, `keep_records`, `max_marks`; environment `FOCAL_GC_INTERVAL_MS`, `FOCAL_GC_GRACE_MS`, `FOCAL_GC_QUARANTINE_MS`, `FOCAL_GC_TERMINAL_MS`), `collecting`, `passes`, and `last` (the pass's `replicas`, `protected_objects`, `opaque_domains`, `bundles_unreadable`, the content store's `content` counts and the `seeds` sweeps). No collection is initiated |
| `cluster gc restore --domain ID --root HASH` | `cluster.gc.restore` | Bring a quarantined content object back with every chunk of it still quarantined, while its round has not expired; `restored: false` when it is already back or no round holds it |
| `cluster backup create --output DIR [--tenant ID] [--session ID]` | `cluster.backup.create` | Write a coherent backup of a hosted native session at its committed prefix ([26 §6](archictecutre/26-custody-archive-retention-and-restore.md)) into a new directory on this node: the replica checkpoints and fsyncs, the envelope, the seed chunks of its root, the exact tree of every object the envelope names and a hashed `MANIFEST` last are written through a durable install sequence. The result names the prefix (legacy and native sequence, Raft index and term, route and epochs), the envelope hash and bytes, the decoder, and the counts of seeds, objects, chunks, bundles, files and bytes. A directory that already holds a backup is refused; a session not yet registered answers `unavailable`; a killed write leaves no manifest |
| `cluster backup verify --input DIR` | `cluster.backup.verify` | Verify a backup without writing and without a running node: the manifest's digest, the envelope's hash, every seed and object file, then the envelope rebuilt against the backup's own files and compared with the manifest's lists; `decoder_supported` says whether this binary carries the backup's decoder; `complete` is true only when every check passed and `problems` is empty |
| `cluster storage show` | `cluster.storage.show` | The storage view of this node ([26 §7](archictecutre/26-custody-archive-retention-and-restore.md)): `disk` (the volume envelope: `free` at the last sample, `outstanding` and `ordinary_outstanding`, `headroom`, `completion_reserve`, and bytes by kind `wal`, `checkpoint`, `content`, `archive`, `staging`), `staged_uploads` and `staged_bytes`, `retire` (the archive agent: `interval_ms`, `grace_ms`, `ticks`, `proposed`, `waiting`, `last_tick_ms`), `gc` (the collector's settings), and `sessions` (per hosted session: `authoritative`, `log_entries_since_checkpoint`, `retention`), `truncated` when more than 256 sessions were left out. No reclamation is initiated |
| `cluster upgrade status` | `cluster.upgrade.status` | The upgrade fence ([24 §21](archictecutre/24-placement-execution-and-fleet-control.md)): the committed level the cluster is held to (zero until activated) with its activation time and registry revision, this binary's level and the one it announces, every directory-listed node with the capability it last reported with its load, and `activatable`, the highest fence every node supports |
| `cluster upgrade activate --fence LEVEL` | `cluster.upgrade.activate` | Raise the fence to `LEVEL` under the founder's authority ([24 §21](archictecutre/24-placement-execution-and-fleet-control.md)) once every listed node reports at least that level; `members_behind` (exit 5) names the nodes reporting less or none; a fence never lowers (`invalid_input`); a fence already at the level reads as done (`changed` false). A node whose binary announces less than the fence refuses to start or stops serving (`upgrade_fenced`, exit 5); `FOCAL_CAPABILITY_LEVEL=N` lowers what a node announces for a rehearsal, never raises it |
| `cluster repair [--tenant ID] [--session ID] [--after ID] [--limit N]` | `cluster.repair` | Repair a hosted session's custody on this node ([24 §20](archictecutre/24-placement-execution-and-fleet-control.md)): the replica exports its committed prefix and the evidence coordinator walks its artifact projection under the session's current placement, verifying every object this node holds, recopying chunk by verified chunk from another required copy (content copies first, then voters) what is missing or fails its hash, recording receipts, and giving other required copies the objects they lack. Missing data is recopied under the same object identity; an object no copy can supply is listed as unrecoverable (bounded; `unrecoverable_count` is exact) and `restore_required` is true. `--limit` bounds the objects one call examines (256 by default, 4096 at most); `complete` says the projection was walked to its end and `next_after` where to resume with `--after`. Idempotent; the export's lease (30 s) bounds one call. A copy the directory is still adding pulls the objects its history names by itself (`custody_objects_missing` in `cluster replicas diagnostics`, `custody_pending` in readiness) |
| `cluster restore --input DIR [--new-incarnation]` | `cluster.restore` | Host the backup's session on this node from its prefix ([26 §6](archictecutre/26-custody-archive-retention-and-restore.md)): the backup is verified first, its objects imported into this node's store and its seeds into the session's seed store, its envelope rewritten for the incarnation the restore takes, a fresh log begun from it, and the session hosted and registered with the directory as founded here at route epoch 1. `decision` is `same_incarnation` (the backup came from this cluster and every other member of its membership is revoked; `fenced_by` names the registry revision) or `recovery_incarnation` (a derived log group and new genesis; `unfenced` names the members that could act again), which `--new-incarnation` must acknowledge. Refused when this node hosts the session, when the directory holds it, or when its tenant is not served. Readers address the restored session with `context add NAME --node-data-dir DIR --tenant ID --session ID` (the operator's local connection) or `context add NAME --enrolled-as CONTEXT --session ID` (an enrolled client, another session of its tenant) |

These observations are local diagnostics. Health does not probe every dependency or establish a quorum. Configuration reports the running owner's saved network coordinates, not the invoking client's settings or an inferred placement policy. The existing top-level `identity` command remains an offline metadata read; the admin identity command reaches the authenticated live owner.

Diagnostics also reports `compiled_native_decoder`, `native_hosted` (the replica holds a content reader and native limits), `native_ready` (its own native floor is durable and promised), `native_active` (the group committed native activation), `native_import_pending` (an import delivery is retained until the host seals its legacy payloads) and `native_authoritative` (native admission is open on this replica). Diagnostics does not initiate a checkpoint, decoder-floor installation or format upgrade. A null required decoder means the group has no fsynced decoder floor at that observation. A durable floor with `managed_active: false` means that decoder requirement is installed but managed admission has not committed activation. `checkpoint_pending` reports current work; it does not invent a last-checkpoint completion receipt. Format transitions still require the explicit compatibility work described in [the lifecycle storage upgrade design](archictecutre/18-lifecycle-storage-upgrade.md).

## Installed application replicas

| CLI | MCP tool | Result |
| --- | --- | --- |
| `cluster replicas list` | `cluster.replicas.list` | Bounded local inventory of installed application owners |
| `cluster replicas show [--session ID]` | `cluster.replicas.show` | Quorum-read application configuration |
| `cluster replicas membership [--session ID] add-learner --node N` | `cluster.replicas.add_learner` | Durably applied learner admission |
| `cluster replicas membership [--session ID] promote --node N` | `cluster.replicas.promote` | Durably applied promotion after catch-up and decoder-support checks |
| `cluster replicas membership [--session ID] remove --node N` | `cluster.replicas.remove` | Durably applied removal |
| `cluster replicas membership [--session ID] leave-joint` | `cluster.replicas.leave_joint` | Durably applied joint-configuration exit |
| `cluster replicas transfer [--session ID] --node N` | `cluster.replicas.transfer` | Transfer initiation under an exact configuration fence |
| `cluster replicas checkpoint [--session ID]` | CLI only until the shared operator descriptors of the deployment work | Checkpoints one installed replica's applied prefix now and compacts its Raft log behind it; refused with `Capacity` while a proposal or delivery is pending. A native Core root larger than the inline bound is sealed as content-addressed seed chunks under `seeds/<session>/` beside the node's data and the checkpoint carries their manifest ([25 §5](archictecutre/25-parallel-materialization-and-ranges.md)) |
| `cluster replicas ranges list [--session ID]` | CLI only until the shared operator descriptors of the deployment work | The committed movement map of a native session ([25 §6–§7](archictecutre/25-parallel-materialization-and-ranges.md)): range epoch, control ordinal, every member's identity, span, generation, holder, readers and row count (the same holders the directory publishes per session under `cluster placement` once a transfer settles), the transfer in progress with its seeds, barrier, seals and readiness, and the retired maps awaiting cleanup |
| `cluster replicas ranges move --member ID --node N [--session ID]` | CLI only until the shared operator descriptors of the deployment work | Begin moving one member to a node; the reply names the transfer (a repeat names the same one). The session leader's controller carries it through the seed, barrier, the holder's readiness stated over its own connection, activation and cleanup; `ranges list` on every node shows the result |
| `cluster replicas activate-native [--session ID]` | CLI only until the shared operator descriptors of the deployment work | Proposes the committed native activation once every voter has promised the native decoder: genesis (authored profile) for an empty ledger, import (projection profile) for a populated one (the node seals inline legacy payloads first). On a laptop node without a network listener the command runs offline against the exclusive data directory and returns after the record is applied and retained |
| `cluster replicas request inspect` | `cluster.replicas.request.inspect` | Latest local application admin intent or receipt |
| `cluster replicas request retry R1` | `cluster.replicas.request.retry` | Retry the identical retained application intent |
| `cluster replicas request reconcile R1` | `cluster.replicas.request.reconcile` | Recover the exact receipt or preserve a fenced unknown outcome |

`cluster replicas activate-native` proposes the `FOCALAC1` activation record described in [native activation and import](archictecutre/23-native-activation-and-import.md); it is refused with a typed outcome while any voter lacks a durable native floor or when the ledger is already native. An empty ledger activates with the authored content profile so claims are authored natively; a laptop node that runs no network listener has no admin socket, so the same command opens the stopped data directory exclusively, commits the record under the node's own authority, checkpoints and reports `activated` and `proposed` in addition to the proposal result (running it again on a native ledger reports `proposed: false`). A populated ledger is imported: the node's content writer seals every inline legacy payload, the replica records the translation root, and every replica reproduces it; a replica whose host has not sealed the payloads yet reports `native_import_pending` until it has. Legacy commands are refused with `Retry` while the record is in flight. The reply acknowledges the proposal; `cluster replicas diagnostics` shows `native_active` once the record is applied. `cluster replicas membership ... show` is a CLI alias for the configuration read. Omitted session selects the node identity's original application session. List accepts `--after SESSION` and `--limit` (1–64, default 32). Its management sequence and continuation describe a live local inventory, not a fixed historical snapshot or placement-authority proof. An initially empty fleet returns an empty inventory; selecting an absent or stopped owner fails explicitly.

Every application operation binds the actual tenant, session, group and current owner incarnation before dispatch. Configuration reads and successful membership replies use the existing current-term read barrier. Metadata changes leave the domain sequence unchanged. A managed-format group still requires the actual candidate's durable decoder support, and promotion still requires replication catch-up; enrollment alone cannot satisfy either condition.

## Exact recovery and limits

Root administration stores one latest intent in `CLUSTER.admin`, protected by `CLUSTER.admin.initialized`. References have the form `a1:<node>:<counter>`. Preserve both directory and marker. Inspect and retry the same reference after a lost reply. Reconcile can recover an exact retained control receipt; a later committed revision plus the root control registry's exact receipt lookup can establish that an uncommitted precondition was superseded. Absence alone remains pending.

Application administration independently stores one latest intent in `REPLICA.admin`, protected by `REPLICA.admin.initialized`. References have the form `r1:<node>:<request-id>`. They bind a distinct request identity and exact configuration precondition. Preserve both filesets. A lost local receipt can be recovered from the application's actual retained membership receipt.

Application consensus retains only its latest membership receipt. If a later operation has replaced that receipt, a newer committed configuration can permanently fence the old request without proving whether it once committed. Reconciliation returns `FencedOutcomeUnknown`; retry will not resubmit it. A new intent may then be created, with a different reference. Previously saved exact receipts remain exact local results. Older references expire when the bounded local journal advances; export results if longer audit retention is needed.

Both journals serialize concurrent processes with private locks, save exact intent before transmission, and fail closed on initialized-state loss or corruption. These are administration references, separate from participant `m1:` streams and legacy domain request identifiers. Transfer replies report initiation and have no durable completion receipt.

Placement drain, migration/custody completion and same-identity credential renewal require additional authority and durable state transitions. They are not exposed as successful aliases for membership removal, invitation issuance or revocation. This administration surface does not claim those workflows have completed.

The executable [CLI/MCP replica regression](../crates/focal-node/tests/support/cli_replicas.rs) checks committed learner admission/removal, unchanged domain sequence and exact receipts after restart. [Owner transfer tests](../crates/focal-node/src/fleet_membership_tests.rs) exercise current-configuration fences; [journal tests](../crates/focal-node/src/cluster_admin/replica_tests.rs) distinguish a lost recoverable receipt from a later fenced unknown outcome.
